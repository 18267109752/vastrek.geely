<resources>

    <style name="GLTheme" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/actionbar_def_style</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
    </style>

    <!-- 自定义ActionBar样式，重设ActionBar背景，隐藏actionbar左侧的应用图标和标题 -->
    <style name="actionbar_def_style" parent="@android:style/Widget.Holo.Light.ActionBar">

        <!-- 在样式里面直接替换掉actionbar的背景，避免在有的窗口进入慢时导致进入时显示系统默认的actionbar -->
        <item name="android:background">@drawable/actionbar_background</item>
        <!-- 在样式里面去掉acionbar的应用图标和标题 -->
        <item name="android:displayOptions">useLogo</item>
    </style>

    <style name="AnimationActivity" mce_bogus="1" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_anim_open_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_anim_open_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/activity_anim_close_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_anim_close_exit</item>
    </style>

    <style name="PopStyle" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/head_in</item>
        <item name="@android:windowExitAnimation">@anim/head_out</item>
    </style>


</resources>